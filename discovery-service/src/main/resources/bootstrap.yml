# Discovery service information
spring:
  application:
    name: discovery-service
  thymeleaf:
    enabled: false
  cloud:
    config:
      uri: ${vcap.services.config-service.credentials.uri}
      fail-fast: true