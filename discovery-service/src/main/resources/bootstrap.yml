# Discovery service information
spring:
  application:
    name: discovery-service
  thymeleaf:
    enabled: false
  cloud:
    config:
      #uri: ${vcap.services.config-service.credentials.uri:http://localhost:1111}
      uri: http://configservice-danielcho.mybluemix.net
      fail-fast: true